{
  "name": "abel-habits",
  "version": "1.0.0",
  "description": "Habit tracking application",
  "type": "module",
  "scripts": {
    "install:all": "npm install && cd api && npm install && cd ../app && npm install && cd ../com && npm install && cd ../dat && npm install",
    "start:api": "cd api && npm start",
    "start:app": "cd app && npm run dev",
    "dev": "concurrently \"npm run start:api\" \"npm run start:app\""
  },
  "keywords": ["habits", "tracking", "productivity"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 